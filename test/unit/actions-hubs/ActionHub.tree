UnitActionHub::constructor
└── when called by a child contract
    └── it sets the parent

UnitActionHub::isChild
├── when the action builder is a child
│   └── it returns true
└── when the action builder is not a child
    └── it returns false

UnitActionHub::_createNewActionBuilder
└── When called
    ├── it creates a new action builder
    ├── it marks the action builder as a child
    └── it emits a NewActionBuilderCreated event
