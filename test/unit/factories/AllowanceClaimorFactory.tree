UnitAllowanceClaimorFactory::createAllowanceClaimor
└── when called
    ├── it should deploy a AllowanceClaimor
    └── it should match the parameters sent to the constructor
    └── it should store the contract as a factory children
