UnitSimpleTransfers::constructor
└── when run
    ├── it should add the transfer to the actions array with correct values
    ├── it should emit the events
    └── it should save the entire array of transfer actions
